{{#heading}}
    <h2>{{heading}}</h2>
{{/heading}}
{{#rows}}
    <ul class="row">
        {{#blurbs}}
            <li class="blurb">
                <date datetime="{{posted_on}}">{{short_date}}</date>
                <a href="{{link}}" title="{{alt}}">
                    <h4>{{title}}</h4>
                </a> 

                {{#summary}}
                    <p class="hyphenate">{{summary}}</p>
                {{/summary}}

                {{#thumbnail}}
                    <img src="{{thumbnail}}" alt="{{title}}" />
                {{/thumbnail}}
            </li>
        {{/blurbs}}
    </ul>
{{/rows}}

<a href="/{{type}}" class="more">See more &rarr;</a>
