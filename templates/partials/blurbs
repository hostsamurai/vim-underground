<h1>{{heading}}</h1>
{{#rows}}
    <ul class="row">
        {{#blurbs}}
            <li class="article">
                <date datetime="{{posted_on}}">{{short_date}}</date>
                <a href="{{link}}">
                    <h4>{{title}}</h4>
                </a> 

                {{#summary}}
                    <p class="hyphenate">{{summary}}</p>
                {{/summary}}

                {{#thumbnail}}
                    <img src="{{thumbnail}}" alt="{{title}}" />
                {{/thumbnail}}
            </li>
        {{/blurbs}}
    </ul>
{{/rows}}
